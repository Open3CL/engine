import { describe, test } from 'vitest';

describe.skip('Application de la méthode 3CL', () => {
  test('test complet de calcul (validation ADEME?)', () => {
    // const dpe = EngineService.execute(originalDpe);=
  });
});
