<svelte:options immutable={true} />

<script lang="ts">import { uniqueId } from '../../utils/uniqueId.js';
import { faCaretDown, faCaretRight } from '@fortawesome/free-solid-svg-icons';
import { debounce, isEmpty, noop } from 'lodash-es';
import Icon from 'svelte-awesome';
import { DEBOUNCE_DELAY } from '../../constants.js';
import { compileJSONPointer, getIn } from 'immutable-json-patch';
import { stringifyJSONPath } from '../../utils/pathUtils.js';
import { transformModalStates, transformModalStateShared } from './transformModalStates.js';
import TransformWizard from './TransformWizard.svelte';
import TransformModalHeader from './TransformModalHeader.svelte';
import AbsolutePopup from './popup/AbsolutePopup.svelte';
import { createDebug } from '../../utils/debug.js';
import TreeMode from '../modes/treemode/TreeMode.svelte';
import { onEscape } from '../../actions/onEscape.js';
import { readonlyProxy } from '../../utils/readonlyProxy.js';
import Modal from './Modal.svelte';
import { onMount } from 'svelte';
import { createHistoryInstance } from '../../logic/history';
const debug = createDebug('jsoneditor:TransformModal');
export let id = 'transform-modal-' + uniqueId();
export let json;
export let rootPath = [];
export let indentation;
export let truncateTextSize;
export let escapeControlCharacters;
export let escapeUnicodeCharacters;
export let parser;
export let parseMemoizeOne;
export let validationParser;
export let pathParser;
export let queryLanguages;
export let queryLanguageId;
export let onChangeQueryLanguage;
export let onRenderValue;
export let onRenderMenu;
export let onRenderContextMenu;
export let onClassName;
export let onTransform;
export let onClose;
let refQueryInput;
const historyInstance = createHistoryInstance({
    onChange: (updatedHistory) => (history = updatedHistory)
});
let history = historyInstance.get();
let selectedJson;
$: selectedJson = readonlyProxy(getIn(json, rootPath));
let selectedContent;
$: selectedContent = selectedJson ? { json: selectedJson } : { text: '' };
let fullscreen = false;
const stateId = `${id}:${compileJSONPointer(rootPath)}`;
const state = transformModalStates[stateId] ?? {};
// showWizard is not stored inside a stateId
let showWizard = transformModalStateShared.showWizard !== false;
let showOriginal = transformModalStateShared.showOriginal !== false;
let queryOptions = state.queryOptions ?? {};
let query = queryLanguageId === state.queryLanguageId && state.query ? state.query : '';
let isManual = state.isManual ?? false;
let queryError = undefined;
let previewError = undefined;
let previewContent = { text: '' };
if (!isManual) {
    updateQueryByWizard(queryOptions);
}
onMount(() => {
    refQueryInput?.focus();
});
function getSelectedQueryLanguage(queryLanguageId) {
    return queryLanguages.find((item) => item.id === queryLanguageId) ?? queryLanguages[0];
}
function updateQueryByWizard(newQueryOptions) {
    try {
        queryOptions = newQueryOptions;
        query = getSelectedQueryLanguage(queryLanguageId).createQuery(selectedJson, newQueryOptions);
        queryError = undefined;
        isManual = false;
        debug('updateQueryByWizard', { queryOptions, query, isManual });
    }
    catch (err) {
        queryError = String(err);
    }
}
function handleChangeQuery(event) {
    query = event.target.value;
    isManual = true;
    debug('handleChangeQuery', { query, isManual });
}
function previewTransform(previewJson, query) {
    if (previewJson === undefined) {
        previewContent = { text: '' };
        previewError = 'Error: No JSON';
        return;
    }
    if (query.trim() === '') {
        previewContent = { json: previewJson };
        return;
    }
    try {
        debug('previewTransform', {
            query
        });
        const jsonTransformed = getSelectedQueryLanguage(queryLanguageId).executeQuery(previewJson, query, parser);
        previewContent = { json: jsonTransformed };
        previewError = undefined;
    }
    catch (err) {
        previewContent = { text: '' };
        previewError = String(err);
    }
}
const previewTransformDebounced = debounce(previewTransform, DEBOUNCE_DELAY);
$: {
    previewTransformDebounced(selectedJson, query);
}
$: {
    // remember the selected values for the next time we open the SortModal
    // just in memory, not persisted
    transformModalStates[stateId] = {
        queryOptions,
        query,
        queryLanguageId,
        isManual
    };
    debug('store state in memory', stateId, transformModalStates[stateId]);
}
function handleTransform() {
    if (selectedJson === undefined) {
        previewContent = { text: '' };
        previewError = 'Error: No JSON';
        return;
    }
    try {
        debug('handleTransform', { query });
        const jsonTransformed = getSelectedQueryLanguage(queryLanguageId).executeQuery(selectedJson, query, parser);
        onTransform([
            {
                op: 'replace',
                path: compileJSONPointer(rootPath),
                value: jsonTransformed
            }
        ]);
        onClose();
    }
    catch (err) {
        // this should never occur since we can only press the Transform
        // button when creating a preview was successful
        console.error(err);
        previewContent = { text: '' };
        previewError = String(err);
    }
}
function toggleShowWizard() {
    showWizard = !showWizard;
    // not stored inside a stateId
    transformModalStateShared.showWizard = showWizard;
}
function toggleShowOriginal() {
    showOriginal = !showOriginal;
    // not stored inside a stateId
    transformModalStateShared.showOriginal = showOriginal;
}
function focus(element) {
    element.focus();
}
function handleChangeQueryLanguage(newQueryLanguageId) {
    debug('handleChangeQueryLanguage', newQueryLanguageId);
    queryLanguageId = newQueryLanguageId;
    onChangeQueryLanguage(newQueryLanguageId);
    updateQueryByWizard(queryOptions);
}
function handleEscape() {
    if (fullscreen) {
        fullscreen = !fullscreen;
    }
    else {
        onClose();
    }
}
</script>

<Modal {onClose} className="jse-transform-modal" {fullscreen}>
  <div class="jse-transform-modal-inner" use:onEscape={handleEscape}>
    <AbsolutePopup>
      <TransformModalHeader
        {queryLanguages}
        {queryLanguageId}
        onChangeQueryLanguage={handleChangeQueryLanguage}
        {onClose}
        bind:fullscreen
      />
      <div class="jse-modal-contents">
        <div class="jse-main-contents">
          <div class="jse-query-contents">
            <div class="jse-label">
              <div class="jse-label-inner">Language</div>
            </div>
            <div class="jse-description">
              <!-- eslint-disable-next-line svelte/no-at-html-tags -->
              {@html getSelectedQueryLanguage(queryLanguageId).description}
            </div>

            <div class="jse-label">
              <div class="jse-label-inner">Path</div>
            </div>
            <input
              class="jse-path"
              type="text"
              readonly
              title="Selected path"
              value={!isEmpty(rootPath) ? stringifyJSONPath(rootPath) : '(document root)'}
            />

            <div class="jse-label">
              <div class="jse-label-inner">
                <button type="button" on:click={toggleShowWizard}>
                  <Icon data={showWizard ? faCaretDown : faCaretRight} />
                  Wizard
                </button>
              </div>
            </div>
            {#if showWizard}
              {#if Array.isArray(selectedJson)}
                <TransformWizard
                  {queryOptions}
                  json={selectedJson}
                  onChange={updateQueryByWizard}
                />
                {#if queryError}
                  <div class="query-error">
                    {queryError}
                  </div>
                {/if}
              {:else}
                (Only available for arrays, not for objects)
              {/if}
            {/if}

            <div class="jse-label">
              <div class="jse-label-inner">Query</div>
            </div>
            <textarea
              bind:this={refQueryInput}
              class="jse-query"
              spellcheck="false"
              on:input={handleChangeQuery}>{query}</textarea
            >
          </div>
          <div class="jse-data-contents" class:jse-hide-original-data={!showOriginal}>
            <div class="jse-original-data" class:jse-hide={!showOriginal}>
              <div class="jse-label">
                <div class="jse-label-inner">
                  <button type="button" on:click={toggleShowOriginal}>
                    <Icon data={showOriginal ? faCaretDown : faCaretRight} />
                    Original
                  </button>
                </div>
              </div>
              {#if showOriginal}
                <TreeMode
                  externalContent={selectedContent}
                  externalSelection={undefined}
                  {history}
                  readOnly={true}
                  {truncateTextSize}
                  mainMenuBar={false}
                  navigationBar={false}
                  {indentation}
                  {escapeControlCharacters}
                  {escapeUnicodeCharacters}
                  {parser}
                  {parseMemoizeOne}
                  {onRenderValue}
                  {onRenderMenu}
                  {onRenderContextMenu}
                  onError={console.error}
                  onChange={noop}
                  onChangeMode={noop}
                  onSelect={noop}
                  onUndo={noop}
                  onRedo={noop}
                  onFocus={noop}
                  onBlur={noop}
                  onSortModal={noop}
                  onTransformModal={noop}
                  onJSONEditorModal={noop}
                  {onClassName}
                  validator={undefined}
                  {validationParser}
                  {pathParser}
                />
              {/if}
            </div>
            <div class="jse-preview-data">
              <div class="jse-label">
                <div class="jse-label-inner">Preview</div>
              </div>
              {#if !previewError}
                <TreeMode
                  externalContent={previewContent}
                  externalSelection={undefined}
                  {history}
                  readOnly={true}
                  {truncateTextSize}
                  mainMenuBar={false}
                  navigationBar={false}
                  {indentation}
                  {escapeControlCharacters}
                  {escapeUnicodeCharacters}
                  {parser}
                  {parseMemoizeOne}
                  {onRenderValue}
                  {onRenderMenu}
                  {onRenderContextMenu}
                  onError={console.error}
                  onChange={noop}
                  onChangeMode={noop}
                  onSelect={noop}
                  onUndo={noop}
                  onRedo={noop}
                  onFocus={noop}
                  onBlur={noop}
                  onSortModal={noop}
                  onTransformModal={noop}
                  onJSONEditorModal={noop}
                  {onClassName}
                  validator={undefined}
                  {validationParser}
                  {pathParser}
                />
              {:else}
                <div class="jse-preview jse-error">
                  {previewError}
                </div>
              {/if}
            </div>
          </div>
        </div>

        <div class="jse-actions">
          <button
            type="button"
            class="jse-primary"
            on:click={handleTransform}
            use:focus
            disabled={!!previewError}
          >
            Transform
          </button>
        </div>
      </div>
    </AbsolutePopup>
  </div>
</Modal>

<style src="./TransformModal.scss">/* over all fonts, sizes, and colors */
/* "consolas" for Windows, "menlo" for Mac with fallback to "monaco", 'Ubuntu Mono' for Ubuntu */
/* (at Mac this font looks too large at 14px, but 13px is too small for the font on Windows) */
/* main, menu, modal */
/* jsoneditor modal */
/* tooltip in text mode */
/* panels: navigation bar, gutter, search box */
/* navigation-bar */
/* context menu */
/* contents: json key and values */
/* contents: selected or hovered */
/* contents: section of collapsed items in an array */
/* contents: highlighting of search matches */
/* contents: inline tags inside the JSON document */
/* contents: table */
/* controls in modals: inputs, buttons, and `a` */
/* messages */
/* svelte-select */
/* color picker */
.jse-transform-modal-inner {
  flex: 1;
  display: flex;
  flex-direction: column;
  min-width: 0;
  min-height: 0;
}
.jse-transform-modal-inner .jse-modal-contents {
  color: inherit;
  flex: 1;
  display: flex;
  flex-direction: column;
  padding: 0;
  overflow: auto;
  min-width: 0;
  min-height: 0;
}
.jse-transform-modal-inner .jse-modal-contents .jse-actions {
  display: flex;
  flex-direction: row;
  justify-content: flex-end;
  padding-top: var(--jse-padding, 10px);
}
.jse-transform-modal-inner .jse-modal-contents .jse-actions button.jse-primary {
  border: none;
  background: transparent;
  color: inherit;
  cursor: pointer;
  font-family: var(--jse-font-family, -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Oxygen-Sans, Ubuntu, Cantarell, "Helvetica Neue", sans-serif);
  font-size: var(--jse-font-size, 16px);
  padding: 5px;
  margin: 0;
  background: var(--jse-button-primary-background, var(--jse-theme-color, #3883fa));
  color: var(--jse-button-primary-color, #fff);
  padding: var(--jse-padding, 10px) calc(2 * var(--jse-padding, 10px));
  border-radius: 3px;
}
.jse-transform-modal-inner .jse-modal-contents .jse-actions button.jse-primary:hover {
  background: var(--jse-button-primary-background-highlight, var(--jse-theme-color-highlight, #5f9dff));
}
.jse-transform-modal-inner .jse-modal-contents .jse-actions button.jse-primary:disabled {
  background: var(--jse-button-primary-background-disabled, #9d9d9d);
}
.jse-transform-modal-inner .jse-modal-contents .jse-main-contents {
  flex: 1;
  display: flex;
  gap: calc(2 * var(--jse-padding, 10px));
  min-height: 0;
  box-sizing: border-box;
  padding: 0 calc(2 * var(--jse-padding, 10px)) var(--jse-padding, 10px);
}
.jse-transform-modal-inner .jse-modal-contents .jse-main-contents .jse-query-contents {
  flex: 1;
  display: flex;
  flex-direction: column;
}
.jse-transform-modal-inner .jse-modal-contents .jse-main-contents .jse-query-contents .jse-description :global(p) {
  margin: var(--jse-padding, 10px) 0;
}
.jse-transform-modal-inner .jse-modal-contents .jse-main-contents .jse-query-contents .jse-description :global(p):first-child {
  margin-top: 0;
}
.jse-transform-modal-inner .jse-modal-contents .jse-main-contents .jse-query-contents .jse-description :global(p):last-child {
  margin-bottom: 0;
}
.jse-transform-modal-inner .jse-modal-contents .jse-main-contents .jse-query-contents .jse-description :global(code) {
  background: var(--jse-modal-code-background, rgba(0, 0, 0, 0.05));
  font-family: var(--jse-font-family-mono, consolas, menlo, monaco, "Ubuntu Mono", "source-code-pro", monospace);
  font-size: var(--jse-font-size-mono, 14px);
}
.jse-transform-modal-inner .jse-modal-contents .jse-main-contents .jse-query-contents .query-error {
  color: var(--jse-error-color, #ee5341);
}
.jse-transform-modal-inner .jse-modal-contents .jse-main-contents .jse-query-contents textarea.jse-query {
  flex: 1;
  outline: none;
  resize: vertical;
}
.jse-transform-modal-inner .jse-modal-contents .jse-main-contents .jse-data-contents {
  flex: 1;
  display: flex;
  flex-direction: column;
  gap: calc(2 * var(--jse-padding, 10px));
}
.jse-transform-modal-inner .jse-modal-contents .jse-main-contents .jse-data-contents .jse-original-data {
  flex: 1;
  display: flex;
  flex-direction: column;
  min-height: 0;
  box-sizing: border-box;
}
.jse-transform-modal-inner .jse-modal-contents .jse-main-contents .jse-data-contents .jse-original-data.jse-hide {
  flex: none;
}
.jse-transform-modal-inner .jse-modal-contents .jse-main-contents .jse-data-contents .jse-preview-data {
  flex: 1;
  display: flex;
  flex-direction: column;
  min-height: 0;
  box-sizing: border-box;
}
.jse-transform-modal-inner .jse-modal-contents .jse-main-contents .jse-data-contents.jse-hide-original-data {
  flex-direction: column;
  gap: 0;
  margin-bottom: 0;
}
.jse-transform-modal-inner .jse-modal-contents .jse-actions {
  padding: var(--jse-padding, 10px) calc(2 * var(--jse-padding, 10px)) calc(2 * var(--jse-padding, 10px));
}
@media screen and (max-width: 1200px) {
  .jse-transform-modal-inner .jse-modal-contents .jse-main-contents {
    flex-direction: column;
    overflow: auto;
  }
  .jse-transform-modal-inner .jse-modal-contents .jse-main-contents .jse-query-contents textarea.jse-query {
    min-height: 150px;
    flex: none;
  }
  .jse-transform-modal-inner .jse-modal-contents .jse-main-contents .jse-data-contents :global(.jse-tree-mode) {
    height: 300px;
    flex: none;
  }
}
.jse-transform-modal-inner .jse-label {
  font-weight: bold;
  display: block;
  box-sizing: border-box;
}
.jse-transform-modal-inner .jse-label .jse-label-inner {
  margin-top: calc(2 * var(--jse-padding, 10px));
  margin-bottom: calc(0.5 * var(--jse-padding, 10px));
  box-sizing: border-box;
}
.jse-transform-modal-inner .jse-label .jse-label-inner button {
  border: none;
  background: transparent;
  color: inherit;
  cursor: pointer;
  font-family: var(--jse-font-family, -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Oxygen-Sans, Ubuntu, Cantarell, "Helvetica Neue", sans-serif);
  font-size: var(--jse-font-size, 16px);
  padding: 5px;
  margin: 0;
  font-weight: bold;
  padding: 0;
}
.jse-transform-modal-inner :global(.jse-tree-mode) {
  flex: 1;
  background: var(--jse-input-background-readonly, transparent);
  box-shadow: none;
  box-sizing: border-box;
  --jse-main-border: var(--jse-input-border, 1px solid #d8dbdf);
}
.jse-transform-modal-inner input,
.jse-transform-modal-inner textarea {
  border: var(--jse-input-border, 1px solid #d8dbdf);
  outline: none;
  box-sizing: border-box;
  padding: calc(0.5 * var(--jse-padding, 10px));
  font-family: var(--jse-font-family-mono, consolas, menlo, monaco, "Ubuntu Mono", "source-code-pro", monospace);
  font-size: var(--jse-font-size-mono, 14px);
  color: inherit;
  background: var(--jse-input-background, var(--jse-background-color, #fff));
}
.jse-transform-modal-inner input:focus,
.jse-transform-modal-inner textarea:focus {
  border: var(--jse-input-border-focus, 1px solid var(--jse-input-border-focus, var(--jse-theme-color, #3883fa)));
}
.jse-transform-modal-inner input:read-only,
.jse-transform-modal-inner textarea:read-only {
  background: var(--jse-input-background-readonly, transparent);
}
.jse-transform-modal-inner .jse-preview.jse-error {
  flex: 1;
  background: var(--jse-input-background-readonly, transparent);
  border: var(--jse-input-border, 1px solid #d8dbdf);
  color: var(--jse-error-color, #ee5341);
  padding: calc(0.5 * var(--jse-padding, 10px));
}
.jse-transform-modal-inner :global(a) {
  color: var(--jse-a-color, #156fc5);
}
.jse-transform-modal-inner :global(a:hover) {
  color: var(--jse-a-color-highlight, #0f508d);
}</style>
