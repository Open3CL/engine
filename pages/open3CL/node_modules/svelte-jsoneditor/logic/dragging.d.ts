import type { JSONPatchDocument } from 'immutable-json-patch';
import type { JSONSelection, RenderedItem } from '../types';
export interface MoveSelectionProps {
    json: unknown;
    selection: JSONSelection | undefined;
    deltaY: number;
    items: RenderedItem[];
}
export interface MoveSelectionResult {
    operations: JSONPatchDocument | undefined;
    updatedSelection: JSONSelection | undefined;
    offset: number;
}
export declare function onMoveSelection({ json, selection, deltaY, items }: MoveSelectionProps): MoveSelectionResult;
